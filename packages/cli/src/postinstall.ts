// Runs after `npm install` as a postinstall script.
// Prints a welcome message so first-timers know what to do next.

console.log(`
  ✦ Penelope installed successfully.

  Get started:
    penelope init          scaffold a new tenant (90-second setup)
    penelope --help        list all commands

  Docs: https://github.com/Mushisushi28/penelope
`);
